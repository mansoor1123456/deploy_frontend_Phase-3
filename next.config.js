/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // appDir is now enabled by default in Next.js 13.4+ and doesn't need to be specified
};

module.exports = nextConfig;